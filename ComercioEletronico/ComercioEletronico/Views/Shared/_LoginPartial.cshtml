@{
    var usuario = Session["Usuario"];

}


@if (usuario != null)
{
    using (Html.BeginForm("LogOff", "Account", FormMethod.Post, new { id = "logoutForm", @class = "navbar-right" }))
    {
        @Html.AntiForgeryToken()
        <ul class="nav navbar-nav navbar-right">

            <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Gerenciador <span class="caret"></span></a>
                <ul class="dropdown-menu">
                    <li>@Html.ActionLink("Cadastro de Produtoras", "Index", "Produtora")</li>
                    <li>@Html.ActionLink("Cadastro de Produto", "Home", "Produto")</li>

                </ul>
            </li>
            <li>@Html.ActionLink("Sair", "Logoff", "Contas")</li>
        </ul>
    }
}
else
{
    
        <ul class="nav navbar-nav navbar-right">
            @*<li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown">Clientes<span class="caret"></span></a>
                <ul class="dropdown-menu">

                    <li>@Html.ActionLink("Cadastro Clientes", "Index", "Usuarios", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
                    <li>@Html.ActionLink("Acessar", "Login", "Usuarios", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>

                </ul>
            </li>*@
            <li>@Html.ActionLink("Cadastro Clientes", "Index", "Usuarios", routeValues: null, htmlAttributes: new { id = "registerLink" })</li>
            <li>@Html.ActionLink("Acesssar", "Login", "Usuarios", routeValues: null, htmlAttributes: new { id = "loginLink" })</li>
            <li>@Html.ActionLink("Área Administrativa", "Index", "Contas")</li>
        </ul>
    
  
}
